
package bootcamp

import com.google.common.collect.ImmutableList
import net.corda.core.contracts.ContractState
import net.corda.core.contracts.StateAndRef
import net.corda.core.identity.Party
import net.corda.core.transactions.TransactionBuilder


class ScratchPad {
    
    fun main(args: Array<String>) {
        /*
        val inputState: StateAndRef<ContractState> = null
        val outputState = HouseState("123 Moorfield Road", null)
        val requiredSigner = outputState.owner.owningKey
        val requiredSigners = ImmutableList.of(requiredSigner)
        val notary: Party = null
        
        val builder = TransactionBuilder()
        
        builder.notary = notary
        
        builder
            .addInputState(inputState)
            .addOutputState(state = outputState, contract = "bootcamp.HouseContract")
            .addCommand(data = HouseContract.Register(), keys = requiredSigners)
        */
    }
    
}
